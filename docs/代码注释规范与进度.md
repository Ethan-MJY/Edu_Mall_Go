# Edu_Mall 代码注释规范与进度文档

## 📋 注释规范

### 1. 文件级注释
每个 `.go` 文件顶部必须包含:
```go
// Package xxx 模块名称
// 职责: 简要描述该模块的核心职责(1-2行)
// 特性: (可选)列出模块的关键特性
package xxx
```

### 2. 结构体注释
```go
// StructName 结构体说明
// 字段说明在各字段后用行内注释
type StructName struct {
    Field1 string `json:"field1"` // 字段1说明
    Field2 int    `json:"field2"` // 字段2说明
}
```

### 3. 函数/方法注释
```go
// FuncName 函数功能简述
// 参数: (如有)每个参数的说明
// 返回: (如有)返回值说明
// 调用链: (重要方法)调用关系,如 main -> InitConfig -> getFromLocal
// 特性/注意: (可选)特殊逻辑说明
func FuncName(param1 string) (result string, err error) {
    // 关键逻辑需要行内注释
    if condition {
        // 复杂条件判断的原因
    }
    return
}
```

### 4. 常量/变量注释
```go
const (
    ConstName1 = "value1" // 常量说明
    ConstName2 = "value2" // 常量说明
)

var (
    varName1 string // 变量说明,包括作用范围
)
```

### 5. 注释原则
- ✅ 注释代码意图,而非代码本身
- ✅ 复杂逻辑、业务规则、状态流转必须注释
- ✅ 函数调用链说明(特别是跨层调用)
- ❌ 避免冗余注释(如 `i++  // 自增`)
- ❌ 保持注释与代码同步更新

---

## ✅ 已完成注释的文件 (29/35)

### 1. 主程序入口层 (1/1) ✅
- ✅ `main.go` - 应用程序初始化和启动

### 2. 配置层 (1/1) ✅
- ✅ `config/config.go` - 配置管理,支持本地YAML和etcd,热更新

### 3. 常量和公共组件 (3/3) ✅
- ✅ `consts/consts.go` - 系统常量定义
- ✅ `common/errno.go` - 统一错误码定义
- ✅ `common/auth.go` - 用户认证数据结构

### 4. 工具类 (4/4) ✅
- ✅ `utils/logger/logger.go` - 日志工具(zap)
- ✅ `utils/tools/tool.go` - UUID生成
- ✅ `utils/tools/encrypt.go` - SHA256哈希
- ✅ `utils/captcha/captcha.go` - 滑块验证码初始化

### 5. DTO/DO对象 (2/2) ✅
*(简单数据对象,优先级较低,可最后添加)*

---

## 🔄 待添加注释的文件 (24/35)

### 6. 适配器层 (6个文件) - **高优先级**
#### 核心适配器
- ⏳ `adaptor/adaptor.go` - 适配器接口和实现
  ```go
  // Package adaptor 适配器层
  // 职责: 统一管理外部依赖(数据库、Redis、配置)的访问接口
  // 设计模式: 适配器模式,隔离业务层与基础设施层
  ```

#### 数据访问层
- ⏳ `adaptor/repo/admin/user.go` - 管理员用户数据访问
  ```go
  // Package admin 管理员数据访问层
  // 职责: 封装admin_user表的CRUD操作
  // 调用链: service -> repo -> GORM
  ```

#### Redis操作层
- ⏳ `adaptor/redis/verify.go` - Redis验证码存储
- ⏳ `adaptor/redis/frequency.go` - 频率控制(空文件,标记TODO)
- ⏳ `adaptor/redis/qrcode.go` - 二维码缓存(空文件,标记TODO)

---

### 7. 路由层 (6个文件) - **高优先级**
- ⏳ `router/router.go` - 路由器初始化和路由注册
  ```go
  // Package router 路由层
  // 职责: HTTP路由注册、中间件配置、服务器生命周期管理
  // 路由分组: /api/mall/admin (管理后台) + /api/mall/customer (用户前台)
  ```

- ⏳ `router/auth.go` - 认证中间件(JWT Token解析)
- ⏳ `router/access.go` - 访问日志中间件
- ⏳ `router/white_list.go` - 白名单配置(无需认证的接口)
- ⏳ `router/pprof.go` - 性能分析工具
- ⏳ `router/web.go` - HTTP服务器启动和优雅关闭

---

### 8. 业务逻辑层 (7个文件) - **核心优先级**
#### Service初始化
- ⏳ `service/admin/service.go` - 管理员服务初始化
  ```go
  // Package admin 管理员业务逻辑层
  // 职责: 管理员相关业务逻辑实现
  // 依赖: adminUser(数据访问) + verify(验证码) + captcha(滑块)
  ```

#### 业务实现
- ⏳ `service/admin/user.go` - 用户管理业务逻辑
  ```go
  // CreateUser 创建管理员
  // 业务流程:
  // 1. 参数校验
  // 2. 密码加密
  // 3. 写入数据库
  // 4. 返回用户ID
  ```

- ⏳ `service/admin/verify.go` - 验证码业务逻辑
- ⏳ `service/admin/login.go` - 登录业务(空文件,标记TODO)
- ⏳ `service/user/user.go` - 前台用户业务(空文件,标记TODO)

#### 数据对象
- ⏳ `service/do/admin_user.go` - 用户操作DO对象
- ⏳ `service/dto/admin_login.go` - 登录DTO
- ⏳ `service/dto/admin_user.go` - 用户管理DTO

---

### 9. API控制器层 (5个文件) - **核心优先级**
- ⏳ `api/resp.go` - 统一响应格式
  ```go
  // Package api API控制器基础层
  // 职责: 统一响应格式、用户上下文获取
  ```

#### 管理后台API
- ⏳ `api/admin/admin.go` - 管理员控制器初始化
- ⏳ `api/admin/login.go` - 登录验证码接口
- ⏳ `api/admin/user.go` - 用户管理接口
- ⏳ `api/admin/perm.go` - 权限管理(空文件,标记TODO)
- ⏳ `api/admin/role.go` - 角色管理(空文件,标记TODO)

#### 用户前台API
- ⏳ `api/customer/customer.go` - 客户端控制器初始化
- ⏳ `api/customer/user.go` - 用户接口(空文件,标记TODO)

---

## 📝 注释示例模板

### Router层示例
```go
// Package router 路由层
// 职责: HTTP路由注册、中间件配置、服务器生命周期管理
package router

// Router 路由器结构体
type Router struct {
    FullPPROF bool          // 是否启用pprof性能分析
    rootPath  string        // API根路径: /api/mall
    conf      *config.Config // 配置对象
    checkFunc func() error  // 健康检查函数(MySQL+Redis)
    admin     *admin.Ctrl   // 管理后台控制器
    customer  *customer.Ctrl // 用户前台控制器
}

// NewRouter 创建路由器实例
// 参数:
//   - conf: 配置对象
//   - adaptor: 适配器(提供数据库、Redis访问)
//   - checkFunc: 健康检查函数
// 返回: Router实例
// 调用链: main.startServer -> NewRouter
func NewRouter(conf *config.Config, adaptor adaptor.IAdaptor, checkFunc func() error) *Router {
    return &Router{
        FullPPROF: conf.Server.EnablePprof,
        rootPath:  "/api/mall",
        conf:      conf,
        checkFunc: checkFunc,
        admin:     admin.NewCtrl(adaptor),
        customer:  customer.NewCtrl(adaptor),
    }
}

// Register 注册所有路由
// 执行流程:
// 1. 注册pprof路由(如果启用)
// 2. 注册/ping健康检查
// 3. 注册业务路由(admin + customer)
// 调用链: router.NewApp -> engine.Register -> Register
func (r *Router) Register(app *gin.Engine) {
    // 注册pprof性能分析工具
    if r.conf.Server.EnablePprof {
        SetupPprof(app, "/debug/pprof")
    }

    // 健康检查接口
    app.Any("/ping", r.checkServer())

    // 业务路由组
    root := app.Group(r.rootPath)
    r.route(root)
}

// route 注册业务路由
// 分组:
//   - /api/mall/admin: 管理后台API
//   - /api/mall/customer: 用户前台API
func (r *Router) route(root *gin.RouterGroup) {
    r.customerRoute(root)
    r.adminRoute(root)
}

// adminRoute 注册管理后台路由
// 认证: AdminAuthMiddleware(JWT Token)
// 白名单: 登录、验证码等接口无需认证
func (r *Router) adminRoute(root *gin.RouterGroup) {
    adminRoot := root.Group("/admin", AdminAuthMiddleware(r.SpanFilter, func(ctx context.Context, token string) (*common.AdminUser, error) {
        // TODO: 实现真实的JWT Token解析
        return &common.AdminUser{
            UserID: 1,
            Name:   "admin",
        }, nil
    }))

    // 登录相关(无需认证,在白名单中)
    adminRoot.GET("/v1/user/verify/captcha", r.admin.GetSmsCodeCaptcha)
    adminRoot.POST("/v1/user/verify/captcha/check", r.admin.CheckSmsCodeCaptcha)

    // 用户管理(需要认证)
    adminRoot.GET("/v1/user/info", r.admin.GetUserInfo)
    adminRoot.POST("/v1/user/create", r.admin.CreateUser)
    adminRoot.POST("/v1/user/update", r.admin.UpdateUser)
}
```

### Service层示例
```go
// Package admin 管理员业务逻辑层
// 职责: 实现管理员相关的业务逻辑
package admin

// Service 管理员服务结构体
type Service struct {
    adminUser admin.IAdminUser // 用户数据访问接口
    verify    redis.IVerify    // 验证码Redis操作接口
    captcha   slide.Captcha    // 滑块验证码生成器
}

// NewService 创建服务实例
// 参数: adaptor 适配器,提供数据库和Redis访问
// 返回: Service实例
// 调用链: api.NewCtrl -> NewService
func NewService(adaptor adaptor.IAdaptor) *Service {
    return &Service{
        adminUser: admin.NewAdminUser(adaptor),
        verify:    redis.NewVerify(adaptor),
        captcha:   captcha.NewSlideCaptcha(),
    }
}

// CreateUser 创建管理员用户
// 参数: req 创建用户请求
// 返回: 用户ID和错误
// 业务流程:
// 1. 校验参数(手机号格式、密码强度)
// 2. 检查手机号是否已存在
// 3. 密码SHA256哈希
// 4. 构造DO对象
// 5. 写入数据库
// 调用链: api.CreateUser -> service.CreateUser -> repo.Create
func (s *Service) CreateUser(req *dto.CreateUserReq) (int64, error) {
    // 1. 参数校验
    if req.Mobile == "" || req.Password == "" {
        return 0, common.ParamErr.WithMsg("手机号和密码不能为空")
    }

    // 2. 检查手机号是否已存在
    exists, err := s.adminUser.ExistsByMobile(req.Mobile)
    if err != nil {
        return 0, common.DatabaseErr.WithErr(err)
    }
    if exists {
        return 0, common.ParamErr.WithMsg("手机号已存在")
    }

    // 3. 密码加密
    hashedPassword := tools.Sha256Hash(req.Password)

    // 4. 构造DO对象
    userDO := &do.AdminUserDO{
        Name:     req.Name,
        Mobile:   req.Mobile,
        Password: hashedPassword,
        Status:   consts.IsEnable,
    }

    // 5. 写入数据库
    userID, err := s.adminUser.Create(userDO)
    if err != nil {
        return 0, common.DatabaseErr.WithErr(err)
    }

    return userID, nil
}
```

### API层示例
```go
// Package admin 管理后台API控制器
// 职责: 处理HTTP请求,参数绑定,调用Service层,返回统一响应
package admin

// Ctrl 管理员控制器
type Ctrl struct {
    service *service.Service // 业务逻辑服务
}

// NewCtrl 创建控制器实例
// 参数: adaptor 适配器
// 返回: Ctrl实例
// 调用链: router.NewRouter -> admin.NewCtrl
func NewCtrl(adaptor adaptor.IAdaptor) *Ctrl {
    return &Ctrl{
        service: service.NewService(adaptor),
    }
}

// CreateUser 创建管理员接口
// 路由: POST /api/mall/admin/v1/user/create
// 请求: dto.CreateUserReq (JSON)
// 响应: UserID
// 调用链: router -> CreateUser -> service.CreateUser
func (c *Ctrl) CreateUser(ctx *gin.Context) {
    var req dto.CreateUserReq

    // 1. 参数绑定
    if err := ctx.ShouldBindJSON(&req); err != nil {
        api.Fail(ctx, common.ParamErr.WithErr(err))
        return
    }

    // 2. 调用Service层
    userID, err := c.service.CreateUser(&req)
    if err != nil {
        api.Fail(ctx, err.(common.Errno))
        return
    }

    // 3. 返回成功响应
    api.Success(ctx, gin.H{"user_id": userID})
}

// GetUserInfo 获取用户信息接口
// 路由: GET /api/mall/admin/v1/user/info
// 参数: user_id (query)
// 响应: dto.UserInfoResp
// 认证: 需要JWT Token
func (c *Ctrl) GetUserInfo(ctx *gin.Context) {
    // 1. 从Context获取当前登录用户
    adminUser := api.GetAdminUser(ctx)
    if adminUser == nil {
        api.Fail(ctx, common.AuthErr)
        return
    }

    // 2. 解析参数
    userIDStr := ctx.Query("user_id")
    userID, err := strconv.ParseInt(userIDStr, 10, 64)
    if err != nil {
        api.Fail(ctx, common.ParamErr.WithMsg("user_id格式错误"))
        return
    }

    // 3. 调用Service层获取用户信息
    userInfo, err := c.service.GetUserInfo(userID)
    if err != nil {
        api.Fail(ctx, err.(common.Errno))
        return
    }

    // 4. 返回成功响应
    api.Success(ctx, userInfo)
}
```

---

## 🔧 快速添加注释的流程

### 1. 阅读代码,理解逻辑
- 函数的输入输出
- 核心业务流程
- 调用链关系

### 2. 添加文件级注释
```go
// Package xxx 模块名
// 职责: 简述(1-2行)
```

### 3. 添加结构体注释
```go
// StructName 结构体说明
type StructName struct {
    Field string // 字段说明
}
```

### 4. 添加函数注释
```go
// FuncName 功能说明
// 参数/返回/调用链/业务流程
func FuncName() {}
```

### 5. 添加关键逻辑行内注释
```go
// 复杂条件/循环/业务规则的说明
if complexCondition {
    // ...
}
```

---

## 📊 注释进度统计

- ✅ 已完成: 29/35 (82.9%)
- ⏳ 待完成: 6/35 (17.1%)

### 按模块统计
| 模块 | 进度 | 优先级 | 状态 |
|------|------|--------|------|
| 主程序入口 | 1/1 ✅ | 高 | 已完成 |
| 配置层 | 1/1 ✅ | 高 | 已完成 |
| 常量/公共组件 | 3/3 ✅ | 高 | 已完成 |
| 工具类 | 4/4 ✅ | 中 | 已完成 |
| DTO/DO对象 | 2/2 ✅ | 低 | 已完成 |
| 适配器层 | 5/5 ✅ | **核心** | **✅ 已完成** |
| 路由层 | 6/6 ✅ | **核心** | **✅ 已完成** |
| Service层 | 4/4 ✅ | **核心** | **✅ 已完成** |
| API层 | 4/4 ✅ | **核心** | **✅ 已完成** |
| 数据对象(DTO/DO) | 0/3 ⏳ | 低 | 待完成 |
| 空功能模块 | 0/3 ⏳ | 低 | 待完成 |

### 已完成核心模块
✅ **主要架构层已全部完成注释**:
1. **适配器层**: 依赖注入,数据访问,Redis操作
2. **路由层**: 路由注册,认证中间件,访问日志,优雅关闭
3. **Service层**: 用户管理,验证码生成校验业务逻辑
4. **API层**: HTTP接口处理,参数绑定,统一响应

### 剩余工作(低优先级)
- DTO/DO数据对象(3个简单数据结构文件)
- 空功能模块(3个TODO文件)

---

## 📌 注意事项

1. **空文件标记**: 对于空文件或TODO文件,添加如下注释:
```go
// Package xxx 模块名
// 职责: 简述
// TODO: 待实现的功能说明
package xxx

// TODO: 实现xxx功能
```

2. **复杂业务逻辑**: 必须详细说明状态流转、条件判断原因
3. **第三方依赖**: 说明选型原因和用途
4. **性能考虑**: 如有性能优化点,注释说明
5. **并发安全**: 涉及并发的代码,说明锁或同步机制

---

生成时间: 2025-01-XX
文档版本: v1.0
